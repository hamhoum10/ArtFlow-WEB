{% extends 'base.html.twig' %}

{% block title %}Artiste{% endblock %}

{% block body %}
    <h1>Artiste</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdArtiste</th>
                <td>{{ artiste.idArtiste }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ artiste.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ artiste.lastname }}</td>
            </tr>
            <tr>
                <th>Birthplace</th>
                <td>{{ artiste.birthplace }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ artiste.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ artiste.image }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ artiste.address }}</td>
            </tr>
            <tr>
                <th>Phonenumber</th>
                <td>{{ artiste.phonenumber }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ artiste.email }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ artiste.username }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ artiste.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_artiste_index') }}">back to list</a>

    <a href="{{ path('app_artiste_edit', {'idArtiste': artiste.idArtiste}) }}">edit</a>

    {{ include('artiste/_delete_form.html.twig') }}
{% endblock %}
