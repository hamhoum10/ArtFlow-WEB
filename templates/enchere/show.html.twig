{% extends 'base.html.twig' %}

{% block title %}Enchere{% endblock %}

{% block body %}
    <h1>Enchere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Ide</th>
                <td>{{ enchere.ide }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ enchere.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ enchere.description }}</td>
            </tr>
            <tr>
                <th>Prixdepart</th>
                <td>{{ enchere.prixdepart }}</td>
            </tr>
            <tr>
                <th>DateLimite</th>
                <td>{{ enchere.dateLimite|date('Y-m-d') }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ enchere.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_enchere_index') }}">back to list</a>

    <a href="{{ path('app_enchere_edit', {'ide': enchere.ide}) }}">edit</a>

    {{ include('enchere/_delete_form.html.twig') }}
{% endblock %}
