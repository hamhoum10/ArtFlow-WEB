<form method="post" action="{{ path('app_parler_delete', {'idComment': parler.idComment}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parler.idComment) }}">
    <button class="btn">Delete</button>
</form>
