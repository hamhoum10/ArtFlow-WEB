{% extends 'base.html.twig' %}

{% block title %}Parler{% endblock %}

{% block body %}
    <h1>Parler</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdComment</th>
                <td>{{ parler.idComment }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ parler.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_parler_index') }}">back to list</a>

    <a href="{{ path('app_parler_edit', {'idComment': parler.idComment}) }}">edit</a>

    {{ include('parler/_delete_form.html.twig') }}
{% endblock %}
