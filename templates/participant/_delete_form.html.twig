<form method="post" action="{{ path('app_participant_delete', {'idp': participant.idp}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participant.idp) }}">
    <button class="btn">Delete</button>
</form>
