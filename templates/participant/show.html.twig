{% extends 'base.html.twig' %}

{% block title %}Participant{% endblock %}

{% block body %}
    <h1>Participant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idp</th>
                <td>{{ participant.idp }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ participant.montant }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('app_participant_index') }}">back to list</a>

    <a href="{{ path('app_participant_edit', {'idp': participant.idp}) }}">edit</a>

    {{ include('participant/_delete_form.html.twig') }}
{% endblock %}
