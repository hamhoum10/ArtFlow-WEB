<form method="post" action="{{ path('app_favori_delete', {'id': favori.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ favori.id) }}">
    <button class="btn">Delete</button>
</form>
